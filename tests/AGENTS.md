# Tests

- Keep domain/application tests independent of WPF, devices, network services, and real user data.
- Persistence tests use isolated temporary directories and assert backward-compatible behavior.
- Architecture tests enforce project dependency direction and keep Infrastructure details out of Core/Application.
- A release claim requires current output; do not copy historical counts from security reports.
