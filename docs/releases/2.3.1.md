# 拆包智录 2.3.1

## 改进

- 修复主页、首次使用页和安装向导中的品牌 Logo 在安装包内无法加载的问题。
- 重新设计系统设置窗口，采用现代卡片式内容、图标侧栏和固定保存操作栏。
- 将相机、存储、单号、异常标签、手机协同、关于和帮助内容重新分组，保留原有配置与兼容行为。

## 兼容性

- 不修改 SQLite、Excel、配置 JSON、录像命名、REST 路径或手机配对协议。
- Android 版本保持 2.3.0；本补丁只更新 Windows 桌面、StationHost 和兼容服务。
- 当前 Windows 安装器未使用可信代码签名，仅作为预发布构建提供。
