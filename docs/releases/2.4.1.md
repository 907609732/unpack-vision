# 拆包智录 2.4.1

2.4.1 是 Windows 桌面端热修复预发布版。

- 修复点击“系统设置”时因缺失 `SettingsTextBox` WPF 样式导致的闪退。
- 机位名称输入框改为使用已声明的统一设置输入样式。
- 增加设置窗口资源引用回归测试，防止同类启动或打开窗口崩溃再次进入发布包。

本版本不改动录像、Excel、SQLite、摄像头或手机配对配置。Windows 安装包在取得可信代码签名前仍仅作为 Prerelease 提供。
